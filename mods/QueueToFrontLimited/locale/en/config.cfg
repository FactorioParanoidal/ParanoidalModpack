[controls]
queue-to-front=Toggle queuing order

[qtf-message]
queue-to-front=Queueing mode set to FRONT
queue-to-back=Queueing mode set to BACK (default)
queue-too-large=ERROR: queue is too large to perform front queueing

[mod-setting-name]
qtfl_queue_limit=Queue Limit

[mod-setting-description]
qtfl_queue_limit=How many recipes in the queue before being blocked adding recipes to front of queue