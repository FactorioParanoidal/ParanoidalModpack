function fillYaiomConfig(config)

	config["yaiom-ferricupric"] = {
		type="resource-ore",
		allotment=80,
		spawns_per_region={min=1, max=1},
		richness=16000,
		size={min=15, max=30},
		min_amount=300,
	}
end